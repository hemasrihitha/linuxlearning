#generate ssh keys the below program generates keys which can be copied to other servers to enable password less accese
ssh-keygen

#this generates keys in home directory of user and default keys are named as id_rsa and id_rsa.pub

#copy the sshkeys to remote server 

ssh-copy-id $USERNAME@$REMOTESERVER

#this copies the id_rsa.pub from sourse server .ssh directory to remote server .ssh/authorized_keys
