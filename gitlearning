#copy ssh keys of localmachine in github ssh section to enable password less cloning of git repo 


-> login to github -> go to user profile -> setting -> ssh and gpg keys -> copy execsting key to new ssh key section

#git work flow in localmachine 

-> git clone $REPO
-> git status
-> git add .
-> git commit -am "$MESSAGE"
-> git push origin
